## 2026-01-14 - Keyboard Navigation Patterns
**Learning:** Even simple static sites benefit significantly from standard keyboard shortcuts (Ctrl+K for search, Esc for modals). These are often overlooked in non-framework projects.
**Action:** Always check for `Escape` key handling on custom modals and consider `Ctrl+K` for search inputs in documentation/reference tools.
